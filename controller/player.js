const { Player } = require('../model/index');

const joinGame = (req, res) => {

  //look up game with booking id

  //check number of players already in game

  //if less then partySize add to Players

}


module.exports = { joinGame }